# Ignore committed legacy static web output.
public/

# Former submodule directory (should not be deployed)
Sparelink/

# Flutter build artifacts
# NOTE: Do NOT ignore `build/` because Vercel's output directory is `build/web`.
.dart_tool/

# IDE
.idea/
.vscode/
